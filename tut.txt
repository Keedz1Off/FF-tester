python3 -m http.server 8080



1.  python3 server.py
2.   python3 -m http.server 8080




Слабая нагрузка:
→ /health
→ /healthz

Средняя:
→ /test-json

Сильная:
→ /test-cpu

Самая тяжёлая:
→ /test-mixed







Тут нет фиксированного числа.
Критично — это не цифра, а поведение.

📊 Признак критического RPS

👉 Когда:

RPS растёт
а сервер начинает тормозить
Конкретные сигналы
RPS растёт + CPU → 90%+
→ сервер на пределе

RPS падает + CPU 100%
→ сервер захлебнулся

RPS скачет
→ нестабильность